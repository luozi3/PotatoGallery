<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>分区管理 ｜ {{ site_name }}</title>
  <meta name="description" content="{{ site_description }}">
  <meta name="theme-color" content="{{ site.theme_color }}">
  {% include "partials/theme_init.html.j2" %}
  <link rel="stylesheet" href="/static/styles/gallery.css?v={{ static_version }}">
</head>
<body class="page-admin page-admin-collections with-sidebar">
  <a class="skip-link" href="#main-content">跳到主要内容</a>
  {% set show_search = false %}
  {% set show_admin_logout = true %}
  {% include "partials/topbar.html.j2" %}

  <main id="main-content" tabindex="-1">
    <div class="home-layout">
      {% include "partials/sidebar_default.html.j2" %}
      <div class="home-main">
        <div class="admin-main">
          <section class="admin-auth admin-surface" data-admin-login>
            <h1>管理员登录</h1>
            <p class="muted">请输入后台账号与密码。</p>
            <form class="admin-form" data-admin-login-form>
              <label>账号</label>
              <input type="text" name="username" autocomplete="username" required>
              <label>密码</label>
              <input type="password" name="password" autocomplete="current-password" required>
              <button class="btn primary" type="submit">登录</button>
              <p class="hint" data-admin-login-error></p>
            </form>
          </section>

          <section class="admin-panel admin-surface admin-collections-panel" data-admin-panel hidden>
            <div class="admin-toolbar admin-hero">
              <div>
                <h1>分区管理</h1>
                <p class="muted">新增/调整分区信息，保存后会同步到站点展示。</p>
              </div>
              <div class="admin-actions">
                <button class="btn ghost" type="button" data-admin-add-collection>新增分区</button>
              </div>
            </div>
            <div class="admin-subnav" aria-label="管理快捷">
              <a class="admin-subnav-link" href="/admin/">管理首页</a>
              <a class="admin-subnav-link" href="/admin/images/">作品管理</a>
              <a class="admin-subnav-link" href="/admin/upload/">上传作品</a>
              <a class="admin-subnav-link" href="/admin/auth/">注册开关</a>
              <a class="admin-subnav-link" href="/admin/tags/">标签管理</a>
              <a class="admin-subnav-link admin-status-link" href="/status/">系统状态</a>
            </div>

            <section class="admin-collections" data-admin-collections>
              <div class="filter-group">
                <span class="label">默认分区</span>
                <select class="select" data-admin-default-collection></select>
              </div>
              <div class="collection-list" data-admin-collection-list></div>
              <div class="collection-actions">
                <button class="btn primary" type="button" data-admin-save-collections>保存分区</button>
                <p class="hint" data-admin-collections-hint></p>
              </div>
            </section>
          </section>
        </div>
      </div>
    </div>
  </main>

  <script src="/static/js/ui.js?v={{ static_version }}" defer></script>
  <script src="/static/js/admin.js?v={{ static_version }}" defer></script>
</body>
</html>
