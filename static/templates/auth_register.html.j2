<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>用户注册 ｜ {{ site_name }}</title>
  <meta name="description" content="{{ site_description }}">
  <meta name="theme-color" content="{{ site.theme_color }}">
  {% include "partials/theme_init.html.j2" %}
  <link rel="stylesheet" href="/static/styles/gallery.css?v={{ static_version }}">
  {% if site.live2d.enabled %}
  <link rel="stylesheet" href="{{ site.live2d.base_url }}/live2d/css/live2d.css">
  {% endif %}
</head>
<body class="page-auth with-sidebar" data-auth-mode="{{ auth.registration_mode }}" data-auth-require-https="{{ 1 if auth.require_https else 0 }}">
  <a class="skip-link" href="#main-content">跳到主要内容</a>
  {% set show_search = false %}
  {% include "partials/topbar.html.j2" %}

  <main id="main-content" tabindex="-1">
    <div class="home-layout">
      {% include "partials/sidebar_default.html.j2" %}
      <div class="home-main">
        <div class="auth-main">
          <section class="card auth-card">
            <h1>用户注册</h1>
            {% if auth.registration_mode == "closed" %}
            <p class="muted">当前暂停注册，请稍后再试。</p>
            {% else %}
            <p class="muted">注册后默认归入 user 分组。</p>
            <form class="admin-form" data-auth-register-form>
              <label>账号</label>
              <input type="text" name="username" autocomplete="username" required>
              <label>密码</label>
              <input type="password" name="password" autocomplete="new-password" required>
              <label>确认密码</label>
              <input type="password" name="password_confirm" autocomplete="new-password" required>
              {% if auth.registration_mode == "invite" %}
              <label>邀请码</label>
              <input type="text" name="invite_code" autocomplete="off" required>
              {% endif %}
              <button class="btn primary" type="submit">注册</button>
              <p class="hint" data-auth-register-error></p>
            </form>
            <p class="muted auth-switch">已有账号？<a href="/auth/login/">去登录</a></p>
            {% endif %}
          </section>
        </div>
      </div>
    </div>
  </main>

  <script src="/static/js/ui.js?v={{ static_version }}" defer></script>
  <script src="/static/js/auth.js?v={{ static_version }}" defer></script>
</body>
</html>
