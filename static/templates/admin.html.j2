<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>管理中心 ｜ {{ site_name }}</title>
  <meta name="description" content="{{ site_description }}">
  <meta name="theme-color" content="{{ site.theme_color }}">
  <link rel="stylesheet" href="/static/styles/gallery.css?v={{ static_version }}">
</head>
<body class="page-admin">
  <a class="skip-link" href="#main-content">跳到主要内容</a>
  <header class="topbar">
    <div class="topbar-left">
      <a class="brand" href="/" aria-label="返回首页">
        <span class="logo-dot" aria-hidden="true"></span>
        <div class="brand-text">
          <strong>luozi_sama</strong>
          <span>Illustration Gallery</span>
        </div>
      </a>
      <button class="icon-button theme-toggle" type="button" data-theme-toggle aria-label="切换主题">
        <span class="icon" aria-hidden="true">◐</span>
      </button>
    </div>
    <nav class="topnav" aria-label="顶栏导航">
      <a href="/">首页</a>
      <a href="/tags/">标签</a>
      <a href="/search/">搜索</a>
    </nav>
    <div class="topbar-right">
      <a class="icon-link" href="/admin/tags/">标签管理</a>
      <button class="icon-button" type="button" data-admin-logout>退出</button>
    </div>
  </header>

  <main id="main-content" class="admin-main" tabindex="-1">
    <section class="admin-auth card" data-admin-login>
      <h1>管理员登录</h1>
      <p class="muted">请输入后台账号与口令。</p>
      <form class="admin-form" data-admin-login-form>
        <label>账号</label>
        <input type="text" name="username" autocomplete="username" required>
        <label>口令</label>
        <input type="password" name="password" autocomplete="current-password" required>
        <button class="btn primary" type="submit">登录</button>
        <p class="hint" data-admin-login-error></p>
      </form>
    </section>

    <section class="admin-panel" data-admin-panel hidden>
      <div class="admin-toolbar">
        <div>
          <h1>作品管理</h1>
          <p class="muted">修改后会自动触发静态刷新，生效有少量延迟。</p>
        </div>
        <div class="admin-actions">
          <button class="btn ghost" type="button" data-admin-refresh>刷新列表</button>
          <button class="btn ghost" type="button" data-admin-toggle-trash>查看垃圾桶</button>
        </div>
      </div>

      <section class="admin-collections card" data-admin-collections>
        <div class="section-header">
          <h2>分区管理</h2>
          <button class="btn ghost" type="button" data-admin-add-collection>新增分区</button>
        </div>
        <div class="filter-group">
          <span class="label">默认分区</span>
          <select class="select" data-admin-default-collection></select>
        </div>
        <div class="collection-list" data-admin-collection-list></div>
        <div class="collection-actions">
          <button class="btn primary" type="button" data-admin-save-collections>保存分区</button>
          <p class="hint" data-admin-collections-hint></p>
        </div>
      </section>

      <section class="admin-controls">
        <input class="admin-search" type="search" placeholder="搜索标题 / 描述 / #标签" data-admin-query>
        <select class="select" data-admin-collection-filter>
          <option value="all">全部分区</option>
          {% for collection in collections_list %}
          <option value="{{ collection.slug }}">{{ collection.title }}</option>
          {% endfor %}
        </select>
      </section>

      <section class="gallery admin-gallery" data-admin-grid data-masonry aria-live="polite"></section>
      <div class="empty" data-admin-empty>当前暂无作品。</div>
    </section>
  </main>

  <script src="/static/js/ui.js?v={{ static_version }}" defer></script>
  <script src="/static/js/admin.js?v={{ static_version }}" defer></script>
</body>
</html>
