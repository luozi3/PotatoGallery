<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>我的作品 ｜ {{ site_name }}</title>
  <meta name="description" content="{{ site_description }}">
  <meta name="theme-color" content="{{ site.theme_color }}">
  {% include "partials/theme_init.html.j2" %}
  <link rel="stylesheet" href="/static/styles/gallery.css?v={{ static_version }}">
</head>
<body class="page-my with-sidebar" data-user-page>
  <a class="skip-link" href="#main-content">跳到主要内容</a>
  {% include "partials/topbar.html.j2" %}

  <main id="main-content" tabindex="-1">
    <div class="home-layout">
      {% include "partials/sidebar_default.html.j2" %}
      <div class="home-main">
        <div class="admin-main">
          <section class="card upload-card">
            <div class="section-header">
              <h1>上传作品</h1>
              <p class="muted">登录后可上传并管理自己的作品。</p>
            </div>
            <form class="admin-form" data-user-upload-form>
              <label>选择图片</label>
              <input type="file" name="file" accept="image/*" required>
              <label>标题</label>
              <input class="admin-input" type="text" name="title" placeholder="可留空">
              <label>正文说明</label>
              <textarea class="admin-textarea" name="description" placeholder="描述/注释"></textarea>
              <label>标签</label>
              <input class="admin-tag-input" type="text" name="tags" placeholder="#tag1 #tag2" data-tag-input data-tag-require-hash="1">
              <p class="hint">发布作品需使用 # 标签，选择子标签后会提示补全父标签。</p>
              <label>分区</label>
              <select class="admin-select" name="collection" data-user-upload-collection>
                <option value="">自动</option>
              </select>
              <div class="admin-actions-row">
                <button class="btn primary" type="submit">开始上传</button>
                <p class="hint" data-user-upload-hint></p>
              </div>
            </form>
          </section>

          <section class="admin-toolbar">
            <div>
              <h2>我的作品列表</h2>
              <p class="muted" data-user-login-hint>未登录时无法加载。</p>
            </div>
          </section>
          <section class="gallery user-gallery" data-user-gallery data-masonry aria-live="polite"></section>
          <div class="empty" data-user-empty>暂无作品，先上传一张吧。</div>
        </div>
      </div>
    </div>
  </main>

  <script src="/static/js/ui.js?v={{ static_version }}" defer></script>
  <script src="/static/js/user.js?v={{ static_version }}" defer></script>
</body>
</html>
